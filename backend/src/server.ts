import app from "./app";
// import { logger } from "./utils/logger";

// const startServer = async () => {
//   try {
//     await app.start();
//   } catch (error) {
//     logger.error("Failed to start the server", error);
//     process.exit(1);
//   }
// };

// startServer();

export default app;
